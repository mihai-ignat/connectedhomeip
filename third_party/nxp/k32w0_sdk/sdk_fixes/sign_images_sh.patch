--- a/sign_images.sh
+++ b/sign_images.sh
@@ -56,7 +56,7 @@ if [ "$#" -gt 0 ]; then
 		MIME_SET="$(file -ib $FILENAME)"
 
 		if [[ $MIME_SET == *"$MIME_PATTERN"* ]]; then
-			python3 $SIGNING_TOOL $FILENAME
+			python3 $SIGNING_TOOL -i=1 $FILENAME
 			arm-none-eabi-objcopy -O binary $FILENAME $FILENAME.bin
 		fi
 	done
